/* var hola= "chau";
var dos = 2;
const url = require('url');

filtro = {
    uruguay : false
}

/* $('.uru').click(function(){
    $.get();
});

document.getElementById("uru").addEventListener("click", uruguay, false);

uruguay = function(){
    console.log("llegue");
    //function(req, res){
    /* const reqUrl = url.parse(req.url, true);
    filtro.uruguay = true;
    console.log(filtro);
    res.statusCode = 200;
    res.end(); 
}

module.exports = {
    uruguay
}; */
//const jsdom = require("jsdom");

/* 
const EventEmitter = require('events');

var hola = function(){
    console.log('en hola');
} 

class MyEmitter extends EventEmitter {}

const myEmitter = new MyEmitter();

myEmitter.once('newListener', (event, listener) => {
    console.log('C');
    if(event == 'event'){
        myEmitter.on('event', () => {
            console.log('B');
        });
    }
});

myEmitter.on('event', () => {
    console.log('A');
});

myEmitter.emit('event'); */